// payfast_probe.js removed — this project now uses the client-side PayFast shortlink embed as requested.
// If you need a server-side probe later, we can reintroduce a safe, ephemeral script that does not store keys.
console.log('payfast_probe.js has been disabled by project configuration.');

